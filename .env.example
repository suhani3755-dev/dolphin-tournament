SECRET_KEY=change-me
# Local default is SQLite in data/tournament.db — leave DATABASE_URL unset on your laptop.
# On Render, set DATABASE_URL in the dashboard to a Neon Postgres URL (never commit the real secret):
# DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require
